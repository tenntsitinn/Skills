interface:
  display_name: "Git 上传前内容与安全审查"
  short_description: "检查提交范围、README、忽略规则及 Git 历史中的凭据泄露风险"
  default_prompt: "请使用 $git-push-review 审查本次拟提交和推送的内容，核对 README、ignore 规则与历史敏感信息，修复授权范围内的问题并报告是否可以上传。"
policy:
  allow_implicit_invocation: true
